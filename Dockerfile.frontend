# Frontend - React 19 + Vite (dev + prod-ready)
FROM node:20-alpine
RUN apk update && apk upgrade --no-cache
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
EXPOSE 5173 4173
CMD ["sh", "-c", "npm install && npm run dev -- --host 0.0.0.0"]
